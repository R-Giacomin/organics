
<div class="container">
  <div class="row mt-2">
    <div class="col">
      <p id="notice"><%= notice %></p>

      <hr>

      <p>
        <strong>Nome do Produtor:</strong>
        <%= @product.user.name %>
      </p>
      <p>
        <strong>Endereço:</strong>
        <%= @product.user.address %>
      </p>

      <hr>

      <p>
        <strong>Produto:</strong>
        <%= @product.name %>
      </p>
      <p>
        <strong>Descrição:</strong>
        <%= @product.description %>
      </p>
      <p>
        <strong>Preço: R$ </strong>
        <%= "#{@product.price} #{@product.unit_measurement}" %>
      </p>

        <% if @product.user != current_user%>
        <%= simple_form_for @order, url: product_orders_path(@product), method: :post do |f| %>
        <%= f.input :quantity %>
        <% end %>


        <%= link_to 'Back', products_path, class: "btn btn-warning" %>


          <% if @product.user == current_user%>
            <%= link_to 'Edit', edit_product_path(@product), class: "btn btn-warning" %>
            <%= link_to 'Destroy', product_path(@product), method: :delete, data: { confirm: 'Are you Sure?' }, class: "btn btn-warning" %>
          <% else %>
            <%= f.submit 'Buy', class: "btn btn-warning" %>
          <% end %>
        <% end %>

    </div>

    <div class="col">
      <%= cl_image_tag @product.photo.key, height: 300, width: 400, crop: :fill %>
    </div>
  </div>
</div>


